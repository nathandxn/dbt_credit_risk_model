[tool.poetry]
name = "dbt_credit_risk_model"
version = "1.0.0"
description = ""
authors = []

[tool.poetry.dependencies]
python = "^3.10.0"

[tool.poetry.dev-dependencies]
[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"